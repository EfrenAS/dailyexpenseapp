export enum charges {
  CARD = 'card',
  CASH = 'cash',
  BANK_CHECK = 'check_banck',
  TRASNFER = 'transfer'
}

export enum typeBill {
  EXPENSE = 'expense',
  DEPOSTI = 'deposit'
}

export enum sqlVerbs {
  SELECT = 'SELECT',
  INSERT = 'INSERT',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE'
}
