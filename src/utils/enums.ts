export enum charges {
  CARD = 'card',
  CASH = 'cash',
  BANK_CHECK = 'check_banck',
  TRASNFER = 'transfer',
  UKNOWN = 'unknown'
}

export enum typeBill {
  EXPENSE = 'expense',
  DEPOSTI = 'deposit',
  UKNOWN = 'unknown'
}

export enum sqlVerbs {
  SELECT = 'SELECT',
  INSERT = 'INSERT',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE'
}
